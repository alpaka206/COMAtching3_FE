# 보안 정책

## 지원 범위

- 기본 브랜치: `main`
- 개발 통합 브랜치: `develop`
- 보안 수정 작업: `fix/*` 또는 `hotfix/*` 브랜치 기준 진행

## 신고 방식

공개 이슈에 토큰, 계정 정보, 취약점 재현 세부 값 기록 금지.
민감 정보가 없는 재현 흐름만 이슈에 기록하고, 실제 시크릿은 별도 비공개 채널로 공유.

## 필수 점검

- XSS: 사용자 입력을 HTML로 직접 삽입 금지
- CSRF: 인증 쿠키 기반 API 변경 시 BE 확인 필요
- 토큰: URL, 로그, localStorage에 토큰 잔존 금지
- 시크릿: `.env`, 빌드 로그, PR 본문 노출 금지
- 의존성: Dependabot 보안 알림 우선 처리
- Firebase: 의존성, 설정, 배포 문서 추가 금지

## PR 기록 기준

보안 영향이 없더라도 PR 본문 `보안/품질 확인` 항목에 확인 결과 기록.
BE API 계약 변경이 필요한 경우 `BE 영향` 항목에 요청/응답 변경 내용 기록.
